<?php

return array(
    // secret for preventing others from modifying your data ("" for no secret)
    "secret" => "",
    // whether or not to require the 'secret' when reading
    "private" => false,
    // path for storing the data file
    "json_path" => "../data/data.json",
);
